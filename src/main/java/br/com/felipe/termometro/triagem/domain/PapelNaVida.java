package br.com.felipe.termometro.triagem.domain;

/**
 * RN-29 — o mesmo gasto, visto pela lente de "que papel isso cumpre na minha vida", não só "dava
 * para cortar". Alimenta o gráfico comparativo (RN-30); não é uma categorização manual nova, é
 * uma leitura sobre o que a triagem (RN-05) e a natureza (fixo/variável) já produzem.
 */
public enum PapelNaVida {
    ESSENCIAL,
    IMPORTANTE_AJUSTAVEL,
    REDUTIVEL,
    EVITAVEL,
    DIVIDA_COMPROMISSO
}
